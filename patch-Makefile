diff -uraN koboldcpp-1.100.1/Makefile omv-koboldcpp-1.100.1/Makefile
--- koboldcpp-1.100.1/Makefile	2025-10-13 10:36:59.000000000 +0200
+++ omv-koboldcpp-1.100.1/Makefile	2025-10-28 21:12:02.063888519 +0100
@@ -172,10 +172,10 @@
 FULLCFLAGS += -mavx2 -msse3 -mssse3 -mfma -mf16c -mavx
 endif # LLAMA_NOAVX2
 else
-CFLAGS += -march=native -mtune=native
-SIMPLECFLAGS += -march=native -mtune=native
-SIMPLERCFLAGS += -march=native -mtune=native
-FULLCFLAGS += -march=native -mtune=native
+CFLAGS +=
+SIMPLECFLAGS +=
+SIMPLERCFLAGS +=
+FULLCFLAGS +=
 endif # LLAMA_PORTABLE
 endif # if x86
 
@@ -360,15 +360,15 @@
 # sve is cooked on termux so we are disabling it
 ifeq ($(UNAME_O), Android)
 ifneq ($(findstring clang, $(CCV)), )
-CFLAGS += -mcpu=native+nosve
-CXXFLAGS += -mcpu=native+nosve
+CFLAGS +=
+CXXFLAGS +=
 else
-CFLAGS += -mcpu=native
-CXXFLAGS += -mcpu=native
+CFLAGS +=
+CXXFLAGS +=
 endif
 else
-CFLAGS += -mcpu=native
-CXXFLAGS += -mcpu=native
+CFLAGS +=
+CXXFLAGS +=
 endif
 endif
 endif
